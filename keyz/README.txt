----COMMANDS----
ENTER either 1, 2, 3, or 4 from the list below

1. Create keys..you need to do this first. PEM files will be created in current directory
2. Use keys to encrypt messages. Once PEM files of keys are made you can start encrypted .txt files.
3. Decrypt messages. Once an ENCRYPTED file is created you can enter file name to see the secret :D.
4. Quit. Always should have an option to terminate program. 